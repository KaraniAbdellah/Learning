// Start Section That Contain Cards and Aside

import Aside from "./Aside";
import Cards from "./Cards";

function Section() {
    return (
        <section className="">
            <Cards/>
            <Aside/>
        </section>
    );
}

export default Section;

